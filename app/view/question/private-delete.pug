extends ../layout/private
include ../mixin/product-item

block title
  title お問い合わせの削除｜オーダーメイド本棚管理システム（デモ）

block breadcrumb
  nav.mt-3(aria-label='パンくずリスト')
    .container-fluid
      ol.breadcrumb
        li.breadcrumb-item
          a(href='../../../') ホーム
        li.breadcrumb-item
          a(href='../../') お問い合わせ
        li.breadcrumb-item
          a(href='../') 詳細
        li.breadcrumb-item
          a(href='./' aria-current='page') 削除

block main
  main
    .container-fluid
      h1 お問い合わせの削除
      nav.mt-3(aria-label='メニュー')
        .d-flex.flex-wrap.gap-2
          a.btn.btn-outline-secondary(href='../') 戻る

      p.mt-3 お問い合わせを削除してもよろしいですか?

      form(role='form')
        .row
          .col
            .d-grid
              a.btn.btn-danger(href='../../delete/finish/') 削除
          .col.order-first
            .d-grid
              a.btn.btn-link(href='../') キャンセル
